#include <fstream>

using namespace std;

ifstream cin("atestat.in");
ofstream cout("atestat.out");

int main()
{
    int n, a[21][21], k;
    cin >> n >> k;
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++){
            
        }

    return 0;
}